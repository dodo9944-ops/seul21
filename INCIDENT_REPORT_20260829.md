# 사건 경위서 (시말서)

**문서 번호**: INC-20260829-001
**작성일자**: 2026-08-29 (KST)
**작성자**: Claude (Anthropic AI Assistant, 세션명 "미경")
**수신**: 대장(사용자)
**사건 분류**: 무관한 파일 변경 사항의 의도치 않은 커밋 포함
**등급**: 경미 (Minor) — 라이브 서비스 장애 없음, 데이터 손실 없음
**최종 상태**: 원인 확인 완료, 재발 방지 조치 반영

---

## 1. 사건 개요

2026-08-29(KST) 18:18경, `(주)빛세움` 홈페이지(`seul24-deploy` 저장소, `backup-bitseum-rebrand-20260824` 브랜치) 작업 중, 사업분야(services.html) 페이지의 중복 코디네이터 섹션을 삭제하는 커밋(`be07fa85`)을 생성하는 과정에서, **작업과 무관한 기존 미커밋 변경 사항(chatbot/chat-expert 관련 파일 삭제, common.js·garoju-service.html 수정)이 같은 커밋에 함께 포함**되었습니다.

라이브 서비스 장애나 데이터 손실은 발생하지 않았으며, 해당 미커밋 변경 사항 자체는 원래 사용자의 정당한 작업(챗봇 기능 제거)으로 확인되어 내용상 문제는 없었으나, **의도하지 않은 범위의 변경이 사용자 승인 없이 함께 커밋된 점**이 문제입니다.

---

## 2. 사건 경위 (타임라인, KST 기준)

| 시각 | 내용 |
|------|------|
| 16:52~17:55 | 회사연혁 이동, 코디네이터 섹션 재배치, 색상/자간/그림자 정리 등 정상 작업 진행 |
| 18:03경 | 대장 지시로 15:02:38(`437e44f6`) 시점 전체 원복 수행. 이 과정에서 세션과 무관한 기존 미커밋 변경 사항(chatbot 삭제분)을 `git stash`로 보존 → 원복 커밋(`a32fd7c3`) → `git stash pop`으로 복원. `garoju-service.html`에서 충돌 발생, 수동 해결 후 **staged 상태로 남음** |
| 18:18경 | 사업분야 페이지 중복 코디네이터 섹션 삭제 지시 수신. `git status`로 기존 staged 변경 사항을 확인하지 않고 `git add redevelopment/pages/services.html && git commit`을 실행 → 이미 staged 되어 있던 무관한 파일들이 같은 커밋(`be07fa85`)에 함께 포함되어 원격 저장소에 푸시됨 |
| 18:20경 | 사용자가 이상 징후를 지적 → 원인 확인 후 사용자에게 즉시 보고 |

---

## 3. 원인 분석

**직접 원인**: 커밋 실행 전 `git status`로 인덱스(staging area) 상태를 확인하지 않음. `git stash pop` 충돌 해결 후 관련 없는 파일들이 이미 staged 상태로 남아 있었는데, 이를 인지하지 못한 채 새 커밋을 생성함.

**근본 원인**: 여러 작업(전체 원복 → 무관한 미커밋 보존/복원 → 신규 기능 삭제)이 짧은 시간 내 연속으로 이어지면서, 각 커밋 직전 `git status` 확인 절차를 생략함.

---

## 4. 조치 내역

- 사용자에게 즉시 원인과 결과를 있는 그대로 보고
- 데이터 손실·라이브 장애 없음을 확인
- 무관하게 포함된 변경 사항(chatbot 삭제분)은 내용상 문제가 없어 되돌리지 않고 유지하기로 사용자 확인

---

## 5. 재발 방지 대책

1. **커밋 직전 `git status` 필수 확인** — 특히 `git stash pop` 또는 병합 충돌 해결 직후에는 반드시 확인 후 커밋 대상 파일만 명시적으로 `git add`
2. **연속 작업 시 커밋 단위 분리** — 원복, 보존/복원, 신규 변경처럼 성격이 다른 작업은 각각 독립된 확인 절차를 거친 뒤 커밋

---

## 6. 비고

본 문서는 사용자 요청에 따라 사실관계만을 기록한 경위서입니다. 실제 피해(데이터 손실, 라이브 장애)는 없었습니다.
